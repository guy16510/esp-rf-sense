{
  "name": "@rf-sense/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Operator CLI: discover devices over mDNS and drive OTA check/apply via the control API.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc --build",
    "discover": "tsx src/discover.ts",
    "ota": "tsx src/ota.ts",
    "status": "tsx src/status.ts"
  },
  "dependencies": {
    "bonjour-service": "1.2.1"
  }
}
